POSTGRES_DB=php_auth
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me

DB_HOST=localhost
DB_PORT=55432

# Сгенерировать: openssl rand -hex 32. Обязателен — приложение не стартует без него.
JWT_SECRET=
JWT_TTL=86400
